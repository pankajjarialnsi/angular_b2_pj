import { AddColorDirective } from './add-color.directive';

describe('AddColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AddColorDirective();
    expect(directive).toBeTruthy();
  });
});
